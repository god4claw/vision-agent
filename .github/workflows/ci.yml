name: CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.25.x'
      - name: gofmt
        run: test -z "$(gofmt -l .)"
      - name: go vet
        env:
          CGO_ENABLED: 1
        run: go vet ./...
      - name: go test
        env:
          CGO_ENABLED: 1
        run: go test ./...
